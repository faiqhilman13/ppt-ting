Optional scratch mode theme templates.

Drop `.pptx` files here to override built-in scratch styling:

- `default.pptx`
- `dark.pptx`
- `corporate.pptx`

If files are missing, scratch mode falls back to python-pptx defaults and applies color palettes in code.
